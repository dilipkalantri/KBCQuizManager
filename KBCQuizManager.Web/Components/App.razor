<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    <link href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/mudblazor@6.19.1/MudBlazor.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="css/app.css" />
    <link rel="icon" type="image/x-icon" href="favicon.ico" />
    <HeadOutlet @rendermode="InteractiveServer" />
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        html, body { font-family: 'Roboto', Arial, sans-serif; background: #1a1a2e; color: #f1f5f9; min-height: 100vh; }
        input, select, textarea, button { font-family: inherit; }
        
        /* Fix for MudBlazor loading indicators */
        .mud-progress-circular { max-width: 50px !important; max-height: 50px !important; }
        .mud-progress-circular svg { max-width: 50px !important; max-height: 50px !important; }
        
        /* Custom spinner styles */
        .kbc-spinner, .kbc-spinner2 {
            width: 50px !important;
            height: 50px !important;
            border: 4px solid #2a3f5f !important;
            border-top: 4px solid #7c3aed !important;
            border-radius: 50% !important;
            margin: 0 auto !important;
            animation: kbc-spin 1s linear infinite !important;
        }
        @@keyframes kbc-spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
    </style>
</head>

<body>
    <Routes @rendermode="InteractiveServer" />
    <script src="_framework/blazor.web.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/mudblazor@6.19.1/MudBlazor.min.js"></script>
</body>

</html>
